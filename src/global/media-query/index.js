const XL_DEVICES = 1366; // laptop atau ipad pro landscape
const L_DEVICES = 1024; // ipad pro potrait atau ipad landscape
const M_DEVICES = 768; // ipad potrait
const S_DEVICES = 480; // mobile

export const XLARGE = `${XL_DEVICES}px`;
export const LARGE = `${L_DEVICES}px`;
export const MEDIUM = `${M_DEVICES}px`;
export const SMALL = `${S_DEVICES}px`;
